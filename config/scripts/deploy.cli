# Script to initialize the postgresql database
# To run: ./jboss-cli.sh --connect --file=deploy.cli

# Start batching commands
batch

# Deploy the war
deploy /var/deployments/transportation-service.war

# Run the batch commands
run-batch
